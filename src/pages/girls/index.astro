---
import BaseListing from "../../layouts/BaseListing.astro";
import { PAGES } from "../../config";

if (PAGES.girls.isActive === false) {
    return Astro.redirect("/404");
}
---

<BaseListing
    title={PAGES.girls.title}
    subtitle={PAGES.girls.subtitle}
    collection="girls"
/>
